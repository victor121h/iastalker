<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Ativar Conta</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: #000;
      color: #fff;
      overflow-x: hidden;
    }

    /* Fundo com efeito código vertical */
    .matrix-bg {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      background: #000; opacity: 1;
      filter: brightness(0.8);
    }

    /* Container principal */
    .content {
      position: relative;
      z-index: 2;
      max-width: 480px;
      margin: 80px auto;
      background: rgba(20, 20, 20, 0.8);
      padding: 40px;
      border-radius: 20px;
      text-align: center;
      box-shadow: 0 0 40px rgba(255, 0, 255, 0.2);
    }

    h1 {
      font-size: 28px;
      line-height: 1.3;
      margin-bottom: 20px;
    }

    p {
      opacity: 0.9;
      font-size: 16px;
      margin-bottom: 30px;
    }

    .button {
      display: inline-block;
      background: linear-gradient(90deg, #ff008c, #ffcc33);
      color: #fff;
      padding: 16px 30px;
      border-radius: 50px;
      font-size: 16px;
      font-weight: bold;
      text-decoration: none;
      transition: 0.3s;
    }

    .button:hover {
      filter: brightness(1.1);
    }

    .subinfo {
      margin-top: 20px;
      font-size: 14px;
      opacity: 0.7;
    }
  </style>
</head>
<body>

  <div class="matrix-bg"></div>

  <div class="content">
    <h1>Ative sua conta agora</h1>
    <p>
      Para ativar sua conta e permitir a instalação do aplicativo no seu celular,
      é necessário pagar uma taxa única de <strong>R$27,90</strong>.
      Isso garante que tudo funcione corretamente, sem erros. Esta é uma taxa obrigatória — caso não seja paga, o acesso à conta não será liberado.
    </p>

    <a href="#" class="button">Ativar Conta</a>

    <div class="subinfo">Pagamento 100% seguro</div>
  </div>

</body>
</html>
